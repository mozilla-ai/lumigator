apiVersion: v1
kind: Service
metadata:
  name: {{ include "lumigator.fullname" . }}
  labels:
    {{- include "lumigator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lumigator.selectorLabels" . | nindent 4 }}
