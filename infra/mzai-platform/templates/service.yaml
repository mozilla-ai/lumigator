apiVersion: v1
kind: Service
metadata:
  name: {{ include "mzai-platform.fullname" . }}
  labels:
    {{- include "mzai-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mzai-platform.selectorLabels" . | nindent 4 }}
